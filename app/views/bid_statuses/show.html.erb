<div class="container mx-auto my-8 px-4">
  <div class="max-w-3xl mx-auto">
    <div class="flex justify-between items-center mb-4">
      <h1 class="h3"><%= link_to "Bid Statuses", bid_statuses_path %> > Bid Status #<%= @bid_status.id %></h1>
      <%= link_to t("edit"), edit_bid_status_path(@bid_status), class: "btn btn-link" %>
    </div>

    <div class="bg-white rounded shadow p-8">
            <div class="mb-4">
        <h5>Name</h5>
        <%= @bid_status.name %>
      </div>
          </div>
  </div>
</div>
